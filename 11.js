// What will be the output of this program?
const user = {
  id: 1,
  firstName: "Rein",
  lastName: "Op 't Land",
  hobbie: "programming",
};

const { firstName, lastName, hobby } = user;

console.log(`${firstName} ${lastName} does ${hobby} for fun`);
